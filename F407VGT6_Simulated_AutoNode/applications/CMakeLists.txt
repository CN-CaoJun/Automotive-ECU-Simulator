# aux_source_directory(. APP_SRCS)
# aux_source_directory(./network APP_SRCS)
# aux_source_directory(./doip APP_SRCS)
# INCLUDE_DIRECTORIES(
#     .
#     ./network
# 	./doip
# )
# add_library(application_lib  ${APP_SRCS})

